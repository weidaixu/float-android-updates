{
  "name": "float-android-mvp",
  "private": true,
  "version": "0.1.0",
  "description": "Android native shell MVP for the Float web application",
  "scripts": {
    "sync": "npx cap sync android",
    "open": "npx cap open android",
    "build:web": "node scripts/build-mobile.mjs",
    "build:android": "npm run build:web && npx cap sync android && cd android && gradlew.bat assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^7.4.3",
    "@capacitor/core": "^7.4.3",
    "@capacitor/local-notifications": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.4.3",
    "typescript": "^5.7.2"
  }
}
